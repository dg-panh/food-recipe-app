import { View, Text } from 'react-native'
import React from 'react'

export default function PlanningCard({ item, index }) {
    console.log(`@plan list:: ${item}`)
    return (
        <View>
            <Text>abc</Text>
        </View>
    )
}